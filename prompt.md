## Rol
Actúa como desarrolladora front-end que enseña a principiantes.

## Contexto
Estoy en un curso y necesito **un solo archivo**  crea un archivo `index.html` que incluya el CSS dentro de `<style>` en el `<head>`. No uso frameworks ni librerías externas.

## Tarea
Diseña una **tarjeta de presentación** simple con:
- un título con mi nombre (usa el placeholder `Daniel`),
- una línea corta de subtítulo (por ejemplo, “Estudiante de …”),
- un botón o enlace ficticio “Contacto” que no haga nada (`href="#"`),
- fondo suave y tipografía legible.

## Formato de salida (sigue este orden; no lo saltes)
1) **Primero y obligatorio:** una sección titulada `## Decisiones de diseño` con **exactamente 3 viñetas** (`-`). Cada viñeta = **una frase** que explique una elección visual o de maquetación (colores, tipografías, espaciados, jerarquía del título, forma de la tarjeta). **No pongas código** en este apartado. Si faltan las 3 viñetas o empiezas por el HTML, la respuesta está incompleta.
2) Comentarios en el código solo donde ayuden a ubicar secciones (máximo 5 líneas de comentarios en total).

## Límites
- No uses JavaScript.
- Máximo ~80 líneas en total (HTML + CSS).
- Colores con buen contraste (texto oscuro sobre fondo claro, o al revés, pero legible).



